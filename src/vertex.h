#pragma once

struct Vertex {
    float x, y;

    Vertex(float xVal, float yVal) : x(xVal), y(yVal) {}
};

struct Vertex2 {
    float x, y;

    Vertex2(float xVal, float yVal) : x(xVal), y(yVal) {}
};
